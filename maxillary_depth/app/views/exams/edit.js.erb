$( document ).ready(function(){ 
    $("div.patient_exam").html("<%= escape_javascript(render partial: 'exams/form', locals: { exam: @exam, url: patient_exam_path(@exam.patient_id, @exam.id), method: :put } ) %>");
    } );

